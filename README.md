# Other Memory

The published source of a homelab's story: what was built, what broke, and what carried over.
Rendered with [Quartz](https://quartz.jzhao.xyz/) by `site/quartz/build.sh` (upstream pinned to
one commit, config overlaid from `site/quartz/quartz.config.yaml`).

This repository is **generated**. Every commit here is one squashed publish of content that
passed an automated leak-gate in a private working repository; nothing is written here by
hand and there is nothing to contribute to here. Issues are off on purpose.
